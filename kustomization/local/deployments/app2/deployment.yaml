apiVersion: apps/v1
kind: Deployment
metadata:
  name: local-web2
spec:
  revisionHistoryLimit: 1      
  selector:
    matchLabels:
      app: local-web2
  # replicas: 1
  template:
    metadata:
      labels:
        app: local-web2
    spec:
      containers:
      - name: local-web2
        image: traefik/whoami
        imagePullPolicy: IfNotPresent